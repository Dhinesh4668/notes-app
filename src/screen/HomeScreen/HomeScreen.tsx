import {Text} from 'react-native';
import {color} from '../../theme/theme';

export const HomeScreen = () => {
  return (
    <Text style={{backgroundColor: color.backgroundColor, flex: 1}}>home</Text>
  );
};
